<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <template id="ssp_dashboard_page">
        <t t-call="web.layout">
            <div class="container mt-5 text-center">
                <div class="card" style="max-width: 600px; margin: 0 auto;">
                    <div class="card-body">
                        <h2>🚀 Smart Solutions Platform</h2>
                        <p class="text-muted">Click the button below to access the platform</p>
                        <hr/>
                        <a t-att-href="ssp_url" 
                           class="btn btn-primary btn-lg" 
                           target="_blank"
                           style="padding: 15px 40px; font-size: 18px;">
                            <i class="fa fa-external-link"></i> Open Smart Solutions Platform
                        </a>
                        <p class="text-muted mt-3" style="font-size: 12px;">
                            It will open in a new window with automatic login
                        </p>
                    </div>
                </div>
            </div>
        </t>
    </template>

    <record id="action_ssp_dashboard_server" model="ir.actions.server">
        <field name="name">SSP Dashboard</field>
        <field name="model_id" ref="model_ssp_config"/>
        <field name="state">code</field>
        <field name="code">
            action = model.action_open_ssp()
        </field>
    </record>

</odoo>
